version: '3.7'

services:
    app:
        build: .
        image: web
        command: python manage.py runserver 0.0.0.0:8000
        volumes:
            - ./code:/code
        ports:
            - '8000:8000'